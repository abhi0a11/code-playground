#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int num = 5;
    // int *p = &num;
    // cout << p << endl;

    // // copying a pointer
    // int *q = p;
    // cout << *q << " - " << *p << endl; // value
    // cout << q << " - " << p<< endl; // address

    // array

    // int arr[10] = {23, 122, 41, 67};

    // cout << "address of first element of array is " << arr << endl;
    // cout << "address of first element of array is " << &arr[0] << endl;

    /*
        arr[i] = *(arr + i);
        i[arr] = *(i + arr);
    */

    // int i = 3;
    // cout << i[arr] << endl;
    // // ERROR
    // //   arr = arr + 1;

    // int *p = &arr[0];

    // character array

    // char ch[6] = "abcde";
    // char *c = &ch[0];
    // cout << c << endl; // wile print entire array till it finds null character.


    // char temp = 'z';
    // char *p = &temp; 
    // cout<<p<<endl; // wile print entire array till it finds null character.

    return 0;
}